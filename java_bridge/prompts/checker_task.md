Verify the following reversed method against the decompilation.

**Method:** ${class_name}::${function_name} at ${address}

**Reversed code:**
```java
${reversed_code}
```

**Decompilation (derived evidence; may contain decompiler errors):**
```
${decompiled}
```

Check every statement, call, branch, loop, and expression order. Return only the JSON object required by the system prompt.
